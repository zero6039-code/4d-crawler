name: Real-time 4D Scraper

on:
  schedule:
    # 每天马时间 19:00 启动 (UTC 11:00)
    - cron: '0 11 * * *'
  workflow_dispatch: # 允许手动点击运行

jobs:
  scrape:
    runs-on: ubuntu-latest
    permissions:
      contents: write # 必须赋予写入权限
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Start 60-Minute Loop Scraper
        run: |
          # 配置 Git 身份
          git config --global user.name "GitHub Action"
          git config --global user.email "action@github.com"
          
          for i in {1..60}
          do
            echo "------------------------------------------"
            # 获取马来西亚当前日期
            DATE=$(date -d "+8 hours" +%Y%m%d)
            echo "正在同步实时开奖 - 轮次: $i - 时间: $(date -d '+8 hours' '+%Y-%m-%d %H:%M:%S')"
            
            # 使用主站 API，这个接口不检查 SAS Token，非常稳定
            curl -sL "https://www.damacai.com.my/callpassresult?pastdate=$DATE" \
              -H "User-Agent: Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/122.0.0.0 Safari/537.36" \
              -o data.json
            
            # 检查抓取结果
            if grep -qi "drawNo" data.json; then
              git add data.json
              # 只有当数据发生变化（出新号）时才推送
              if git commit -m "Live Update: $(date -d '+8 hours' '+%H:%M:%S')"; then
                git push
                echo "✅ 发现新号码，网页已同步！"
              else
                echo "😴 数据暂无变化。"
              fi
            else
              echo "⚠️ 抓取内容无效。前50字符: $(head -c 50 data.json)"
              echo "正在尝试备用方案..."
            fi
            
            echo "等待 60 秒进行下轮抓取..."
            sleep 60
          done
